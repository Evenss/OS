1. sem_t *sem_open(const char *name,int oflag,mode_t mode,unsigned int value);	创建并初始化有名信号量
	name 信号量的外部名字
	oflag 选择创建或打开一个现有的信号灯，O_CREAT（创建一个信号量）
	mode 权限位
	value 信号灯初始值

2. int shmget(key_t key, size_t size, int shmflg); 
	第二个参数，size以字节为单位指定需要共享的内存容量

3. void *shmat(int shm_id, const void *shm_addr, int shmflg);  用来启动对该共享内存的访问，并把共享内存连接到当前进程的地址空间
	第一个参数，shm_id是由shmget函数返回的共享内存标识。
	第二个参数，shm_addr指定共享内存连接到当前进程中的地址位置，通常为空，表示让系统来选择共享内存的地址。
	第三个参数，shm_flg是一组标志位，通常为0

4. int shmdt(const void *shmaddr);  用于将共享内存从当前进程中分离
	参数shmaddr是shmat函数返回的地址指针
